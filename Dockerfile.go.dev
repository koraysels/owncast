FROM golang:alpine

RUN apk add --no-cache gcc build-base linux-headers ffmpeg ffmpeg-libs

RUN go install github.com/air-verse/air@latest

WORKDIR /app

# Pre-download modules at build time (network available here, not at runtime)
COPY go.mod go.sum ./
RUN go mod download
